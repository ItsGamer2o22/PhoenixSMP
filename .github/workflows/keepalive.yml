name: PhoenixSMP KeepAlive Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */14 * * *" # Run every 14h to keep it alive

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout repo
        uses: actions/checkout@v3

      - name: ⚡ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: 📦 Install dependencies
        run: |
          if [ -f package.json ]; then
            npm install
          else
            echo "⚠️ No package.json found, skipping npm install"
          fi

      - name: 🚀 Start PhoenixSMP Bot (24/7)
        env:
          MC_SERVERS: ${{ secrets.MC_SERVERS }}
          MC_USERNAME: PhoenixBotSMP
        run: |
          echo "Starting bot in keepalive mode..."
          FILE=$(ls | grep -i '^bot\.js$' | head -n 1 || true)
          if [ -z "$FILE" ]; then
            echo "❌ No bot.js or Bot.js found in repo root!"
            exit 1
          fi
          while true; do
            node "$FILE" || echo "⚠️ Bot crashed, restarting in 10s..."
            sleep 10
          done
